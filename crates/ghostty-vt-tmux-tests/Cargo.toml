[package]
name = "ghostty-vt-tmux-tests"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dev-dependencies]
ghostty-vt = { path = "../ghostty-vt", features = ["std", "terminal-owned"] }
