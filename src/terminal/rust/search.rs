//! Search functionality for the terminal.
//!
//! Port of `src/terminal/search.zig`. Re-exports search types from
//! sub-modules. Stub structs are provided until those sub-modules are ported.

pub struct Active;
pub struct PageList;
pub struct Screen;
pub struct Viewport;
